# Whether the plugin is enabled, if this is 0 then none of the other configuration settings do anything
enabled = 1

# Specify the fields which need replacement
duplicate_replacements = {
	# table name
	#ca_objects = {
	#	# record type (or __default__)
	#	__default__ = {
	#		# field name
	#		idno = {
	#			#specify the regular expression for the search
	#			# Must be preg_replace compatible
	#			pattern = /^(\\w+)-*\\d+$/,
	#			# Specify the replacement pattern. Note you have to escape curly braces
	#			replace = $\{1\}000000,
	#			# Specify the hook.
	#			# Current implemented hooks are: hookBeforeBundleInsert, hookBeforeDuplicateItem, hookDuplicateItem
	#			hook = hookBeforeBundleInsert,
	#			# Message for the user.
	#			#    optional
	#			message = _(Replaced old Accession Number with %2. Please ensure that you change the accession number to match your new record.),
	#			# Message level. One of info, warn, error
	#			#    optional defaults to 'info'
	#			message_type = warn
	#		}
	#	}
	#}
}
